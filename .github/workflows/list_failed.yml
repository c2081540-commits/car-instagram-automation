name: List Failed Stories

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  list-failed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Show failed Stories
        run: python failed_posts.py list
